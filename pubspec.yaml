name: auto_pop_scope
description: "앱 전체에 자동으로 PopScope를 적용하여 iOS 팝 제스처를 모든 페이지에서 인식할 수 있게 해주는 패키지입니다."
version: 0.0.1
homepage: https://github.com/choe-inho/auto_pop_scope
repository: https://github.com/choe-inho/auto_pop_scope
issue_tracker: https://github.com/choe-inho/auto_pop_scope/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
